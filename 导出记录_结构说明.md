# 导出记录与结构说明

## 1. 当前对话关键记录

### 用户需求演进
- 先要求修复“切换图表后空白”的问题。
- 明确提出：必须基于以下数据文件进行完整数据分析可视化：
  - `23-26山本投手数据.csv`
  - `23-26大谷打者数据.csv`
- 要求实现的模块包括：
  - Pitching Modules
    - Pitch Chart
    - Radial Chart
    - Detailed Zone Chart
    - Pitch Heatmap
    - Pitch Types By Count
    - Pitch Types By Location
    - Pitch Breakdown
    - Release Point Chart
    - Pitch Movement Chart
    - Hexbin Pitch Chart
    - Count Breakdown
  - Batting Modules
    - Spray Chart
    - Launch Angle Chart
    - Zone Profile
    - Spray Heatmap
    - EV vs LA
- 设计风格要求：
  - 参考 `@vendor/airbnb`
  - 参考 `@vendor/apple`
- 后续进一步要求：
  - 把 `Pitching` / `Batting` 模块拆成独立图表组件
  - `Home / Compare / Story` 保持独立
  - 每个模块只渲染自己的图表组件
  - 不再复用同一个 `ref`
  - 用代码完成数据分析与可视化

### 我已完成的主要工作
- 阅读并分析了 `src/App.tsx` 的结构。
- 新增了 `src/charts.tsx`，用于承载独立图表组件。
- 开始把图表逻辑从 `App.tsx` 分离到独立模块中。
- 已将部分图表改成“单组件单容器”的思路。
- 已完成对部分数据解析逻辑的增强，使其能更稳定地读取 CSV 字段。

### 当前阻塞点
- `src/App.tsx` 里还残留旧逻辑：
  - 重复的 `ref`
  - 残留的旧 `useEffect`
  - 导入名与 `src/charts.tsx` 导出名不一致
- 目前需要继续统一：
  - `charts.tsx` 的导出名
  - `App.tsx` 的导入名
  - 模块切换与图表分发结构

## 2. 当前代码结构说明

### `src/App.tsx`
该文件当前仍是主页面入口，包含：
- 顶部导航与页面切换
- `Home / Pitcher / Batter / Compare / Story` 页面结构
- 数据加载（CSV 读取）
- 旧版总览图表逻辑
- 仍有部分图表模块的旧 ref / effect 残留

### `src/charts.tsx`
该文件用于承载独立图表组件，目标是把图表模块化为：
- `Pitching` 相关组件
- `Batting` 相关组件
- 每个组件独立挂载、独立渲染、独立数据筛选

### 数据文件
- `23-26山本投手数据.csv`
- `23-26大谷打者数据.csv`
- 以及页面中仍使用到的球队级 CSV 数据

## 3. 建议的最终结构

### 页面层
- `App.tsx`
  - 负责页面状态、导航、数据加载、模块切换
  - 负责渲染 `Home / Compare / Story`
  - 负责将投打模块分发到子组件

### 图表层
- `src/charts.tsx`
  - 负责所有 Pitching / Batting 图表组件
  - 每个模块独立一个图表组件
  - 每个组件内部独立管理 `ref` 和 ECharts 实例

### 组件分发方式
- Pitching：
  - `Pitch Chart`
  - `Radial Chart`
  - `Detailed Zone Chart`
  - `Pitch Heatmap`
  - `Pitch Types By Count`
  - `Pitch Types By Location`
  - `Pitch Breakdown`
  - `Release Point Chart`
  - `Pitch Movement Chart`
  - `Hexbin Pitch Chart`
  - `Count Breakdown`
- Batting：
  - `Spray Chart`
  - `Launch Angle Chart`
  - `Zone Profile`
  - `Spray Heatmap`
  - `EV vs LA`

## 4. 备注

如果需要，我下一步可以继续把 `App.tsx` 和 `charts.tsx` 的导出/导入完全对齐，并把模块分发结构收尾完成。